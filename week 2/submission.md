in this week 2 project i created a research engine same as perplexity which calls tools if it requires one for answering questions of user
i created a web search function and then web fetch fuction there is also a function named fetch clean which uses trafilatura to clean the  response
this agent can call 4 tools which are:
web_search
web_fetch
discover_papers
get_paper_content
i also used textual to convert this into a perplexityapp
there are also a few shortcuts to use actions such as clear history,clear interface,exit model 
now we can finally run the file and use the engine
