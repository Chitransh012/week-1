in this project i created a research agent that can use different tools such as web search for searching web it can also search google using serper it can also do various files operations such as writing a file , editting a file , deleting a file , appending some content to an existing file it can also search research papers using 2 functions paper search and read paper
i made this agent like it can be used in 2 ways we can simply use it by running it in our terminal and also be used via terminal user interface tui
my main code is in agent.py which imports tools from another folder

