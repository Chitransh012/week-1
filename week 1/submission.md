I am Chitransh Chhaniwal, in this chatbot i first of all created a class chatagent and gave user options to choose from 3 models which are openrouter, gemini or deepseek.
my chatagent class has diffrent functions which are assigned different jobs like i 1st made call model function which generates a response of ai on getting prompt.
i also prepared rolling_buffer function whose main function was to summarise if length of messages exceeded max allowed limit(which user can choose) it does it either automatically or user can also type "/compact" to summarise history.
And the main function was start ai which gets activated on calling it takes prompt of user continuously and generates reply of ai and also does some other function like exiting from chat or clearing chat or compacting chat or seeing number of tokens and when ai replies it append user prompt and ai reply to messages list and continues loop it always check for limit of max of allowed messages by calling rolling buffer every time prompt is passed.
**Now i also made some other function which was not given in weekly task i also made a function which verifies answer of ai i passed on it list of messages and reply of ai and user prompt of that reply and it checks reply of ai by using openrouter and tells whether reply is incorrect or correct if reply is incorrect than in start ai function that user prompt is removed from messages and a new prompt has to be written . I made this because when i passed a prompt to this "my name is miek" and then in next prompt i asked ai what is my name it replied "your name is mike" i dont know why so i made this model which verifies the reply on ai.**
now finally when user calls the start ai function when passing name of agent loop starts.
This is all. 